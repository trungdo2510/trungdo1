#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int pin,card;
	int isOK;
		
		printf("\nNhap ma pin : ");
		scanf("%d",&pin);
		printf("\n Ma card nhap la : ",card);
		scanf("%d",&card);
		
	if(pin==1234 && card==6789)
		
	return 0;
} 
